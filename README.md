# Dark Wizard Kiyoshi

This project now uses webpack to bundle the game scripts. To build the bundle run:

```bash
npm install
npm run build
```

The compiled file will be written to `dist/bundle.js` and is referenced from `home.html`.
